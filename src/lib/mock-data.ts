import type {
  Company,
  Network,
  Conversation,
  Need,
  AppNotification,
} from "./types";

export const networks: Network[] = [];
export const companies: Company[] = [];
export const companyById = () => undefined;
export const ME = "";
export const needs: Need[] = [];
export const conversations: Conversation[] = [];
export const notifications: AppNotification[] = [];
